package repository

import (
	"github.com/long250038728/web/tool/app"
)

type {{serverNameFunc .Server}} struct {
	util *app.Util
}

func New{{serverNameFunc .Server}}Repository(util *app.Util) *{{serverNameFunc .Server}} {
	return &{{serverNameFunc .Server}}{util: util}
}
